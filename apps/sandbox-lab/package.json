{
  "name": "@anpord/sandbox-lab",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "bun test --pass-with-no-tests",
    "ingest": "bun run src/ingest.ts",
    "run": "bun run src/run.ts"
  },
  "dependencies": {
    "@daytonaio/sdk": "^0.205.0",
    "e2b": "^2.9.0"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
