{
  "name": "@anpord/docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run sync",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "preview": "bun run build && mint dev --port 3007",
    "sync": "bun run scripts/sync-openapi.ts",
    "check:links": "mint broken-links"
  },
  "devDependencies": {
    "mint": "4.2.51"
  }
}
