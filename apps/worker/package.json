{
  "name": "@anpord/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_NO_WARNINGS=1 trigger dev --profile anpord",
    "smoke": "bun --env-file=.env run scripts/smoke.ts",
    "deploy": "trigger deploy",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "bun test --pass-with-no-tests"
  },
  "dependencies": {
    "@anpord/db": "workspace:*",
    "@anpord/eval": "workspace:*",
    "@anpord/ids": "workspace:*",
    "@trigger.dev/sdk": "4.5.15",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@aws-sdk/client-secrets-manager": "^3.1136.0",
    "@trigger.dev/build": "4.5.15",
    "trigger.dev": "4.5.15",
    "typescript": "catalog:"
  }
}
