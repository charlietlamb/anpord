export const BATCH_RUNS_TABLE = {
  columns: "minmax(0,14rem) minmax(0,16rem) minmax(0,1fr) 7.5rem 5.5rem 1rem",
  headings: ["Case", "Variant", "Progress", "Status", "Time"],
  label: "Runs in this batch",
} as const;
